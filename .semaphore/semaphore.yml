version: v1.0
name: First pipeline example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "try cloning"
    task:
      jobs:
      - name: Submodules
        commands:
          - cat ~/.ssh/config
          - checkout
          - ls -1
          - ssh-add -L
